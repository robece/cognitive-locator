<?xml version="1.0" encoding="UTF-8"?>
<views:BaseView x:Class="CognitiveLocator.Views.HomeView" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:views="clr-namespace:CognitiveLocator.Views;assembly=CognitiveLocatorPCL" Title="{Binding Title}">
    <Grid Margin="20,0">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition  Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" HorizontalOptions="Center" VerticalOptions="End" FontSize="30" Text="¿Podemos ayudarte?"  />
        <Button Grid.Row="1" Text="Deseo reportar a alguien" FontSize="20" Command="{Binding SearchPersonCommand}" />
        <Button Grid.Row="2" Text="Deseo buscar a alguien" FontSize="20" Command="{Binding CreateReportCommand}" />
    </Grid>
</views:BaseView>