<?xml version="1.0" encoding="UTF-8"?>
<views:BasePage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="CognitiveLocator.Views.LanguageSettingsPage" 
    xmlns:views="clr-namespace:CognitiveLocator.Views" Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Style="{DynamicResource CommonVerticalStackLayout}">
            <Label Style="{DynamicResource TitleText}" Text="{Binding Settings_Language}" />
            <Picker Style="{DynamicResource TextColor}" Title="{Binding Settings_LanguageSelect}" ItemsSource="{Binding Languages}" SelectedIndex="{Binding LanguagesSelectedIndex}" />
        </StackLayout>
    </ContentPage.Content>
</views:BasePage>