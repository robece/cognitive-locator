<?xml version="1.0" encoding="UTF-8"?>
<views:BaseView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:CognitiveLocator.Views;assembly=CognitiveLocatorPCL"
    x:Class="CognitiveLocator.Views.SearchPersonResultPage"
    Title="{Binding Title}">
	<ContentPage.Content>
            <ListView
                ItemsSource="{Binding Results}"
                RowHeight="80"
                ItemSelected="Handle_ItemSelected"
                >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell Text="{Binding Name}" Detail="{Binding Location, StringFormat='Ubicación: {0}'}" ImageSource="{Binding Picture}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
	</ContentPage.Content>
</views:BaseView >
